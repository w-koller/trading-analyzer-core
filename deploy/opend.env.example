# /opt/opend/opend.env
# Not committed to git — create this by hand on the LXC (or have
# setup_app.sh copy it from a non-tracked location).
# chmod 600 this file: it identifies which moomoo account OpenD logs into.
MOOMOO_LOGIN_ACCOUNT=your_moomoo_id_email_or_phone
